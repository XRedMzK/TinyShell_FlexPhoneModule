# TinyShell_FlexPhoneModule

FlexPhone module repository.

## Stack
- Client: Tauri 2 + React + TypeScript
- Server: FastAPI + WebSocket signaling
- Infra/Auth: Redis-backed state, WebRTC/coturn, JWT/challenge auth

## Current State
- Progress tracking is maintained in `progress.md`.
